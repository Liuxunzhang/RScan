[package]
name = "rscan-web"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
anyhow.workspace = true
encoding_rs = "0.8.35"
regex = "1.12.2"
reqwest = { version = "0.12.24", default-features = false, features = ["blocking", "gzip", "rustls-tls", "socks"] }
rscan-config = { path = "../config" }
