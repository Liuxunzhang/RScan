# RScan 当前执行计划

## 当前目标

优先把 **端口扫描** 和 **服务指纹识别** 做扎实，继续收敛与原始行为的关键差异；`POC`、`EXP`、大部分 `plugin` 逻辑暂时后置，不再作为当前主线。

## 当前原则

1. 仓库内不再保留 `.go` 源文件
2. 与端口扫描、指纹识别无关的功能先不扩展
3. 每次改动都补回归并验证
4. 每轮结束后整理摘要、提交并推送 `dev`

## 当前优先级

1. **端口扫描**
   - 端口字符串解析继续向原始行为收敛
   - 扫描并发、超时、错误容忍、结果排序保持稳定
   - 优先保证 `net/`、`config/`、`core/` 这一条链路正确

2. **服务指纹识别**
   - 去掉对旧 Go 资产文件命名的依赖
   - 将端口映射、规则资源逐步收敛为 Rust 侧资源
   - 优先保证 `fingerprint/` 的探针顺序、端口映射、结果字段稳定

3. **仓库去旧特征**
   - 文档、输出名、资源名、平台产物名统一为 `rscan`
   - 清掉旧项目路径和文件命名残留

4. **后置项**
   - `plugin` 的进一步实现与扩展
   - `POC` 引擎和 `EXP` 利用链的重构

## 本轮重点

- 已完成端口解析对非法 token 的容错收敛
- 已完成输出文件追加语义对齐
- 已移除仓库中的 `.go` 资产文件，统一改为 Rust 资源路径
- 下一步优先处理：
  1. 指纹识别资源的 Rust 化收敛
  2. 哈希文件混合合法/非法行的容错
  3. 继续清理剩余旧命名和兼容文案

## 提交要求

- Git 作者固定使用：
  - `liuxunzhang`
  - `1927971759@qq.com`
- 每轮改动后都需要：
  1. 写明本轮实现内容
  2. 提交到当前分支
  3. 推送到远端 `dev`
