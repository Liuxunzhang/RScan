# RScan 重构执行计划

## 目标

以 Rust 持续完善 `RScan`，优先完整复现原扫描器的**端口扫描**、**服务/WEB 指纹识别**与 **POC** 能力，并保持 CLI、默认行为、输出结构、结果字段尽量兼容。当前阶段不再把利用链挂在主流程里，先把扫描底座做实、做稳、做干净。

## 当前阶段重点

### 1. 端口扫描主链路

- 目标解析、主机展开、CIDR / IP 段 / 文件输入兼容
- 主机存活探测与 `-np` / `-ping` 语义
- TCP 端口扫描、默认端口组、排除端口、并发与超时控制
- `host:port` 直达目标兼容
- 输出稳定的 `PORT` 结果

### 2. 指纹识别主链路

- `nmap-service-probes` 解析与探针调度
- 端口到 probe 的映射与默认 probe 行为
- banner 清洗、unknown 兜底、TLS fallback 等兼容细节
- Web 标题抓取、协议探测、重定向、编码处理
- Web 指纹规则匹配与 POC 映射所需的基础数据

### 3. 代码与仓库清理

- 移除旧 Go 资源文件与 Go 风格内嵌资源
- 统一为 Rust 常量、Rust 模块或 Rust 数据表
- 清理旧项目命名与遗留特征
- 利用模块与扫描主流程解耦，当前阶段不包含利用链模块
- 保持仓库可直接作为独立 Rust 项目构建、测试、提交

## 已完成

### 扫描与运行时

- 已完成 CLI / config / core / net / fingerprint / web / output / platform workspace 拆分
- 已接通主机发现、端口扫描、Web 探测、服务识别、本地模式等主执行流
- 已补齐多项运行时兼容行为：
  - `FS_ARGS` 优先级
  - `host + url` 混合模式
  - 模式大小写敏感
  - `-local` 调度全部本地模块
  - 输出追加写入语义
  - 无效端口 token 跳过而不中断

### 指纹与 Web

- 已接入 `nmap-service-probes` 解析和默认 probe 调度
- 已补齐 unknown、HTML/HTTP 兜底、SMB banner、TLS fallback 等关键兼容点
- 已完成 Web 标题、重定向、`Content-Length`、GBK 回退、指纹命中与 POC 选择优先级修正

### 仓库清理

- 仓库中已移除 `.go` 文件
- 以下资源已改为真实 Rust 数据文件，不再保留 Go 语法内容：
  - `fingerprint/assets/port_map.rs`
  - `web/assets/rules.rs`
- 对应调用方已改为直接消费 Rust 常量，而不是解析 Go 文本
- 默认 Web / POC `User-Agent` 已改为常见浏览器头
- 已移除利用链主流程入口、相关运行参数与内置资产，当前仅保留扫描、指纹与 POC 主线

## 当前待办

### A. 继续补齐端口扫描兼容

1. 复查发现层剩余边角行为
2. 继续对齐端口组、排除规则、超时与异常容错
3. 增强端到端回归，锁定常见扫描输入组合

### B. 继续补齐指纹识别兼容

1. 复查服务指纹与端口映射结果差异
2. 对齐 hash / 输入文件等影响识别链路的容错行为
3. 补更多指纹回归，重点覆盖常见服务和 Web 指纹

### C. 延后事项

以下工作保留到后续阶段，不作为当前主线：

- 大规模 `plugin` 扩展
- `POC` 全量兼容
- `EXP` / 利用链拆分后重构

## 执行原则

1. **优先兼容外部行为**：CLI、默认值、输出结构优先保持稳定。
2. **优先扫描底座**：先完善端口扫描与指纹识别，再扩展漏洞与利用能力。
3. **优先清理遗留特征**：新增实现不再引入 Go 代码、旧项目命名或路径依赖。
4. **每次改动都要可回归**：任何兼容修正都要配套测试，避免后续回退。

## 提交要求

- Git 作者使用：`liuxunzhang <1927971759@qq.com>`
- 每次完成一批可验证改动后：
  1. 更新必要代码与文档
  2. 运行相关测试
  3. 提交到 `dev` 分支
  4. 推送到 `https://github.com/Liuxunzhang/RScan`
